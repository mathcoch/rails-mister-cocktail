<h1 class="text-center">Details of </br><strong><%= @cocktail.name %></strong></h1>

<ul>Ingredients and doses:
  <% @cocktail.doses.each do |dose| %>
    <li>Ingredient: <%= dose.ingredient.name %> |
        Dose: <%= dose.description %> |
        <%= link_to dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } do %>
          <i class="fa fa-trash" aria-hidden="true"></i>
          <% end %>
</li>
  <% end %>
</ul>


<h2>Add an ingredient and dose</h2>
<%= render 'doses/form' %>

